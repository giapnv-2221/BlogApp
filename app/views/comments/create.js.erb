$("#comment_content").val("");
$(".comments").html("<%= j render partial: 'comment', collection: @comments %>");
$(".paging-rails").html("<%= j paginate @comments, theme: 'twitter-bootstrap-3', remote: true, params: {controller: 'entries', action: :show, id: @entry.id} %>");
